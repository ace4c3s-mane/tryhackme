
Finding the domains:

```bash
assetfinder --subs-only uber.com
```

Checking what domains run http server . Output the output and save the rest to the hosts file

```bash
cat domains | httprobe | tee hosts
```


James Kettle @albinowax - Turbo intruder

```bash
grep -Hnri <subdomain> *  | vim -
```
  


Vim to shell to vim


```bash
%!sort -u
%!grep -v Content-Security
```

```bash
xargs takes 
```

 https://tomhudson.co.uk/